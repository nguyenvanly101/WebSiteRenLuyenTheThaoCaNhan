@model WorkoutLogFormViewModel
@{
    ViewData["Title"] = "Tao Workout Log";
}

<section class="surface-card">
    <p class="eyebrow">Create Log</p>
    <h1>Ghi nhan buoi tap moi</h1>
    <form asp-action="Create" method="post" class="form-stack mt-4">
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly" class="validation-summary"></div>
        <partial name="_LogForm" model="Model" />
        <div class="actions-row">
            <button type="submit" class="button">Luu buoi tap</button>
            <a class="button button--ghost" asp-action="Index">Quay lai</a>
        </div>
    </form>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
